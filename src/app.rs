pub struct AppState {
    // TODO: Add fields for app state (e.g., file list, git status, user config)
}

pub fn run() {
    // Initialize app state
    let _state = AppState {
        // Fill with initial values later
    };
    // Start TUI
    crate::tui::start_tui();
}
